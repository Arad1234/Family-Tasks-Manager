import { Typography } from '@mui/material';
import styled from 'styled-components';

export const StyledSendEmailMessage = styled(Typography)`
	text-align: center;
	color: white;
	font-size: 20px;
	line-height: 2rem;
	width: 90%;
	word-wrap: break-word;
`;

export const StyledForgotPasswordTitle = styled(Typography)`
	font-size: 25px;
	text-align: center;
	width: 75vw;
	font-weight: 500;
`;
