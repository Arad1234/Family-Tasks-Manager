import { Typography } from "@mui/material";

const LoginTitle = () => {
  return (
    <Typography
      sx={{
        display: "block",
        fontSize: "29px",
        fontWeight: "600",
      }}
    >
      Welcome Back!
    </Typography>
  );
};

export default LoginTitle;
